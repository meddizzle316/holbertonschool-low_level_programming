#include <stdio.h>
#include <stdlib.h>
/**
 * function name - function description
 * @parameters: description
 * Return: return value
 */
int main (void)
{
	int i;

	for (i = 0; i < 15; i++)
	{
		printf("%d\n", rand());
	}
	return (0);
}
