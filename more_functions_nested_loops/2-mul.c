#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: 1
 */
int mul(int a, int b)
{
	if (a > 47 && a < 58 && b > 47 && b < 58)
	{
		a * b; 
	}
}
